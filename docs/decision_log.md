# Decision Log

| Date | Decision | Rationale | Status |
|---|---|---|---|
| 2026-09-07 | GitHub-first product repository | Avoid ad-hoc build drift and make AI-assisted build controllable | Accepted |
| 2026-09-07 | PWA-first | Avoid native app-store complexity in Alpha-50 | Accepted |
| 2026-09-07 | Next.js + FastAPI | Portable frontend/backend split | Accepted |
| 2026-09-07 | Rules versioned in config, not buried in UI | Auditability and governance | Accepted |
| 2026-09-07 | Fulfilment gated | Legal/LSP/DLA risk | Accepted |
| 2026-09-08 | Alpha-50 reconciliation uses one borrowing policy, one Money Value service, and one shared audit contract | Prevent duplicate decision logic and preserve traceability across both journeys | Accepted |
| 2026-09-08 | Product events are minimal server-recorded events | Support journey learning without PII, financial values, or third-party analytics | Accepted |
| 2026-09-14 | Track 1.1 adds a reveal, intent, closure and terminal continuation flow on top of the existing quick-check results | Preserve the result experience while collecting one explicit future-use signal per journey | Accepted |
| 2026-09-14 | Continuation now opens as a replacement screen only after explicit user action, and is blocked on stale results | Avoid mixed result/continuation context and prevent using future-step flows on outdated estimates | Accepted |
| 2026-09-14 | Money Value reward inputs support cashback amount, points/miles conversion, and explicit unknown valuation paths | Expand user-declared compatibility without adding external data dependencies and without treating unknown as zero | Accepted |
| 2026-09-17 | 1.1A controlled build keeps raw financial inputs transient by default and uses replaceable local storage adapters | Preserve privacy boundaries now while enabling a future approved durable storage implementation without rewriting journey logic | Accepted (PR #5) |
| 2026-09-17 | Borrow illustrative rate is configurable at 14% p.a. effective 2026-09-17 with explicit non-offer wording | Freeze a deterministic prototype baseline owned by product while avoiding hardcoded duplicated rates | Accepted (PR #5) |
| 2026-09-17 | Rewards annualization supports monthly, quarterly and yearly periods; unknown points/miles value shows annualized quantity only | Enforce deterministic period math and prevent synthetic INR conversion when representative value is unknown | Accepted (PR #5) |
| 2026-09-18 | Audit events store redacted, schema-versioned snapshots (`audit-redacted-v1`) instead of raw financial inputs | Keep audit traceability without retaining raw financial values in logs | Accepted (PR #6) |
| 2026-09-19 | PostgreSQL 16 with SQLAlchemy and Alembic is the durable persistence foundation; Phase A is infrastructure only, with `/health` (liveness) separated from `/ready` (database readiness) | Establish migrations, engine lifecycle and PostgreSQL-backed CI before any business-domain tables or behaviour change | Accepted (PR #7) |
| 2026-09-20 | Anonymous continuity uses a server-issued HttpOnly session cookie with a fixed 90-day absolute expiry (no sliding extension), exact-origin credentialed CORS and origin validation on state-changing cookie-authenticated routes | Give anonymous journeys continuity without identity capture, and bound session lifetime and cross-origin exposure | Accepted (PR #8) |
| 2026-09-20 | Anonymous product events, journey runs, attribution and continuation intents are persisted in PostgreSQL scoped to the anonymous session, with bounded-batch retention purge; a repeated continuation action in the same journey run keeps its product event and does not create a second continuation-intent row | Preserve real product events while keeping the continuation projection unique per session, run, event type, intent and reason, without failing the request | Accepted (PR #8) |
| 2026-09-20 | Anonymous-session token rotation remains dormant in Phase B. Invocation after OTP verification and history linking is deferred to Track 1.1B. Rotation must not extend the session's original absolute-expiry boundary. | Keep rotation as a tested internal primitive with no browser-accessible endpoint until identity linking exists, and prevent rotation from becoming a way to extend the 90-day absolute lifetime | Accepted |
| 2026-09-20 | `ROADMAP.md` is the strategic source of truth; `docs/execution/DELIVERY_GATE.md` is the pre-merge and release control standard; `docs/execution/FIRST_10_ISSUES.md` is the original seed backlog only | Remove ambiguity between a stale roadmap, a seed backlog and a candidate governance checklist | Accepted (PR #10) |
| 2026-09-20 | Concurrent work is allowed only with controlled ownership: one owner per task, an isolated git worktree per concurrent task and non-overlapping file scopes declared up front | Replace the blanket single-agent/no-worktree rule with a control that matches how work is actually done, while still preventing overwritten work | Accepted (PR #10) |
| 2026-09-20 | Final journey authority is the approved Rewards and Borrow Better PNGs plus `docs/product/journeys/JOURNEY_FLOW_SPEC.md`; all prior issue and handover assumptions are superseded where they conflict | Keep the product journey ordering authoritative, especially before any implementation PR for 1.1A or 1.1B proceeds | Accepted (PR #14) |
| 2026-09-20 | Future data-contract, event-taxonomy and Privacy Notice dependencies are required before any connected journey or permission expansion, but they remain deferred until separate approval gates | Preserve the anonymous-only flow while making the dependency chain explicit for future 1.1B and 1.2 work | Accepted (PR #14) |
| 2026-09-20 | Product events gain an optional categorical `screen_name` limited to eight values (`rewards_card_behaviour`, `rewards_priorities_inputs`, `rewards_check`, `rewards_connected_example`, `borrow_monthly_position`, `borrow_plan`, `borrow_check`, `borrow_connected_example`), paired with its own journey and stored in a nullable check-constrained column. Two event types are added: `result_declared` and `connected_example_seen`. `step_viewed` and `step_completed` keep `screen_name` optional and all historical event types stay accepted. Step 5 renders no pilot CTA until Phase 1.1B. | Let the Rewards and Borrow 1.1A journeys emit distinguishable step, result and example events independently, with no free-text or value-bearing field and without breaking existing producers | DECIDED |
| 2026-09-20 | Rewards 1.1A Step 4 evidence-bounded copy. This written decision supersedes the literal final-PNG wording only for the Step 4 "Spending fit" and "Our nudge" copy. Step 4 shows the priorities the customer selected and states that category fit cannot be determined from the declared inputs alone. The nudge is `Compare the rewards you actually receive with the annual fee and any interest you pay.` Step 4 does not recommend a particular card and does not state that the customer's current card is unsuitable. Separately, the Step 5 sentence `You may earn more with a card that rewards dining.` is accepted only inside the fixed synthetic example, because the persistent banner says `ILLUSTRATIVE EXAMPLE — NOT YOUR DATA`, the example never uses customer inputs, and it names no particular card or product. It must not be reused as personalized Step 4 advice. All other final journey authority is unchanged. | The declared-data journey does not collect the card's category-specific earning structure, so Sutriva cannot validly claim that the customer's card does not fit their dining, travel, grocery or bills priorities, and must not imply that switching cards is recommended | DECIDED |
| 2026-09-20 | Borrow Better 1.1A server-authoritative illustrative rate. The canonical customer-facing copy is `Illustrative annual rate: 14%. Configured by policy; not a loan offer.` The rate is display-only and never customer-editable, and there is no "Change rate" control. The backend-configured policy rate is authoritative for every calculation. If a client omits the rate, the backend applies the configured rate. If a client supplies the exact configured rate, the request may be accepted for backward compatibility. If a client supplies a different rate, the request returns `422`, and a mismatched client rate is never used in any calculation. Step 4 displays the rate echoed by the backend response. The Step 3 frontend may initially display the bundled value from the same shared policy configuration (`shared/track11_config.json`), but that value is provisional until the backend EMI preview responds and must never be treated as an independent calculation authority. Production deployment must keep the PWA and API policy configuration synchronized, and PWA/API rate-configuration consistency is a release gate and a monitoring responsibility. This decision intentionally supersedes the previous behaviour that honoured arbitrary client-supplied rates. | The final Borrow journey defines the illustrative rate as policy-controlled. Allowing customers or callers to change the calculation rate would contradict the journey, make displayed comparisons inconsistent and risk presenting results under an unauthorised assumption. | DECIDED |
| 2026-09-21 | Frontend upgraded to Next.js 15.5.25, React and React DOM 19.3.0 and Vitest 4.1.11 to clear the critical and high Next.js and Vitest advisories reported on `next@14.2.23`. The one remaining high finding, `postcss@8.4.31` bundled by Next, is accepted only for private UAT because builds process trusted first-party CSS. It remains a blocker requiring re-evaluation before any public pilot. No `overrides` or audit suppressions are used, and Next.js 16 is deferred. | Remove the exploitable runtime advisories now without combining the work with a two-major framework migration, and keep the residual build-time finding visible as a gate | Accepted (PostCSS finding private UAT only) |
| 2026-09-21 | AWS is the approved platform for the private 1.1A UAT, replacing the earlier Vercel, Railway, App Runner and Amplify options, none of which was the deployed topology. The topology is same-origin `https://uat.sutriva.io` on separate Lightsail resources in `ap-south-1`: instance `small_3_1` (2 GB) running nginx, the Next.js PWA and FastAPI, with nginx routing `/v1/*`, `/health` and `/ready` to FastAPI and everything else to Next.js, plus a private, encrypted PostgreSQL 16 database on bundle `micro_2_0` (not PostgreSQL 14 or the default 18). Access is protected by nginx Basic Auth plus nginx rate limits. The recurring budget ceiling is USD 32/month. Build-time flags are `NEXT_PUBLIC_TRACK_11A_ENABLED=true` and `NEXT_PUBLIC_TRACK_11B_ENABLED=false`; the committed JSON flags stay false/false, and Phase 1.1B stays disabled. The production `sutriva-alpha`, `sutriva-alpha-ip` and `sutriva.io` are out of scope. No public launch is authorized by this decision, and no UAT resource has been created or deployed yet. | Give private UAT a genuine access restriction, a durable PostgreSQL 16 store and same-origin cookies on the smallest isolated footprint, without touching production or widening scope | Accepted (private UAT only; no public launch) |
| 2026-09-21 | A temporary, access-restricted Phase 1.1A closed production canary is approved on the existing `sutriva-alpha` host at `https://sutriva.io`, as a narrowly defined exception that supersedes the "production is out of scope" rule of PR #20 only for this canary. (1) Access is limited to the owner and named testers using nginx Basic Auth; there is no Instagram traffic, public advertising, public launch or unrestricted access. (2) Only Phase 1.1A is enabled through the build-time variables `NEXT_PUBLIC_TRACK_11A_ENABLED=true` and `NEXT_PUBLIC_TRACK_11B_ENABLED=false`; the committed JSON flags stay false/false. Phase 1.1B, mobile capture, OTP, Twilio and consent remain prohibited. (3) The existing private PostgreSQL 16 `sutriva-uat-db` is the isolated canary database and is not a general production customer database. (4) Python 3.11 and Node 22 may be installed additively: system Python 3.9 is unchanged, the legacy release stays on Node 18, and the new release invokes `/usr/bin/node-22` explicitly. (5) Builds happen off-host in Linux/amd64, so no `npm ci` or `next build` runs on the nano instance. The legacy release `7c20ba6c4e9ae384d4c7e361c2e78f00bd6e9066` and the shared venv are left untouched, and the new release uses a separate directory and a per-release Python 3.11 venv. (6) Basic Auth, `X-Robots-Tag: noindex, nofollow`, nginx rate limits and protected API routing must be active before the new application becomes reachable, and the ACME certificate-renewal challenge path must keep working without weakening access to the application. (7) The existing nginx and systemd files, the legacy release and `/tmp/sutriva/*.jsonl` are backed up before any deployment mutation, and an instance snapshot and a database snapshot must reach a successful terminal state before migration or release activation. (8) Rollback is a symlink reversal and sequential service restart; the database is not downgraded. Activation aborts and rolls back if `MemAvailable` falls below 60 MB, an OOM occurs, `/ready` fails, the homepage or a journey fails, an access control is absent or events do not persist; a package transaction is never killed once its RPM transaction has begun. (9) The residual PostCSS finding (one high and one derivative moderate) is accepted only for this authenticated, named-tester canary and remains a blocker requiring re-evaluation before any unrestricted or Instagram pilot. (10) The `door_selected` navigation-loss issue is outside the six-record journey contract and does not block this canary, but remains a public-pilot analytics blocker. (11) The USD 32/month ceiling still applies, and production modifications outside this approved procedure remain prohibited. The separate Lightsail UAT remains the preferred longer-term environment. This decision deploys nothing and authorizes no public launch. | Avoid waiting for the ineffective Lightsail instance quota while retaining access control, isolation, rollback and measurable abort gates. The host, runtimes, memory headroom, database reachability and application were verified read-only or off-host before this decision. | Accepted (temporary closed canary only; no public launch) |
| 2026-09-25 | PR #26 (merge commit `5eb2317f1e2bfed9da07f56df4f80f63766cf5b5`) deployed to the closed canary as a **code deployment only**. Phase 1.1B's Step 6 pilot-interest code ships in this release but stays fully disabled: the committed `shared/track11_config.json` is unchanged (`track11aEnabled=false`, `track11bEnabled=false`), and the frontend was built with `NEXT_PUBLIC_TRACK_11A_ENABLED=true` / `NEXT_PUBLIC_TRACK_11B_ENABLED=false`, matching every prior canary build. Every `/v1/pilot/*` route returns `404` (confirmed with valid payloads, not just malformed ones); no Step 6 UI, mobile-number field, OTP interface or Twilio reference appears in any served page or built JS chunk. Migration applied to `sutriva-uat-db` **only**: `0003_product_event_screen_name → 0004_pilot_registration → 0005_otp_send_log (head)`, adding `pilot_registrations`, `otp_challenges` and `otp_sends` — all three confirmed empty post-deployment, and no forbidden 1.1B/pilot event type exists anywhere in `product_events`. Pre-deployment instance snapshot `sutriva-alpha-pre-pr26-5eb2317-20260925T112234Z` and database snapshot `sutriva-uat-db-pre-pr26-5eb2317-20260925T112234Z` both reached `available` before any mutation. Smoke-tested: `/health` and `/ready` (`migration: current`), the core Borrow Better rate-config endpoint (confirms the illustrative-rate config loads correctly, which the deployment process initially broke — see the lesson below), a full six-record Borrow event contract verified by direct query, and external unauthenticated access still refused (`401`) with `/docs` still `404` and `noindex` still present. Rollback set preserved: previous release `a65808bc12c13ba950cbc2a06a7b0dfb35c75f05` (PR #25) untouched at `/opt/sutriva/releases/`; rollback is the documented symlink reversal, and the database is not downgraded. PR #27 (a Twilio SMS adapter for the still-disabled pilot flow) was **not** deployed and is confirmed absent from this release's tree. No SMS was sent. Two deployment lessons recorded from this rollout: (a) `/tmp` on `sutriva-alpha` is RAM-backed tmpfs (~210 MB) on a 419 MB-total instance — staging a large build artifact there dropped `MemAvailable` below the 60 MB abort threshold before any service was touched; build artifacts must be extracted straight to `/opt/sutriva/releases/...` (real disk), never staged at more than a few MB in `/tmp`; (b) the deploy process must copy `shared/track11_config.json` into every new release directory — omitting it doesn't only affect 1.1B, it breaks the core Borrow Better rate-config endpoints too, since the backend reads that file from the release root unconditionally. Separately, during post-deployment smoke testing an operator credential file was read to test authenticated access and was exposed in a session transcript through a redaction error; that credential has since been **rotated** on the host and the exposed local file removed, and the rotation was verified (old value rejected, new value accepted) without the new credential ever appearing in chat, a shell command, or this log. Unrelated Twilio credentials that were stored in the same exposed file were rotated separately by the account owner directly in the Twilio Console and were never used by any deployment step. | Ship the reviewed, security-hardened Phase 1.1B code to the existing canary host now, under the same flag-off discipline as every prior deployment, so a future decision to enable it needs only a flag flip and a real SMS-provider integration rather than a fresh deployment; keep the credential-exposure incident and its containment in the same durable record as the deployment it happened during. | Accepted (code deployment only; 1.1B remains disabled; canary access unchanged) |
| 2026-09-26 | Phase 1.1A's Borrow Better and Money Value routes are replaced by a real, responsive nine-situation redesign (Rising EMIs, New purchase, Loan offer, Rejected/shortfall, Annual fee, Card fit, Carrying a balance, Several cards, Unused points), built from the reviewed final web mockup as design/copy reference only. All calculation moves server-side (`services/api/app/services/situations.py`, migration `0006_situation_screen_names`); a visitor reaches any situation either by choosing it on a group landing page or via a campaign URL (`?situation=<key>`) that opens it directly; every result carries one of three provenance labels (untouched example / example-plus-edits / the visitor's own declared figures), never a single blanket "illustrative only" label; each situation states its own specific limitation next to its result; no entered financial value is ever placed in a URL or an analytics event. Ships behind the existing `track11aEnabled` flag, unchanged mechanism; the committed flags stay `false`/`false`, and `LegacyBorrowBetterPage`/`LegacyMoneyValuePage` remain the exact flag-off fallback. See `docs/execution/SITUATIONS_REDESIGN.md`. | Turn the reviewed mockup into the actual customer-facing 1.1A experience — landing through question, inputs and result — rather than a standalone prototype or a copy patch, while keeping the accuracy, provenance and phase-boundary constraints that gate this phase | DECIDED |
| 2026-09-26 | Consolidated product correction (Copilot review + founder decisions): each situation's static, non-interactive "a future pilot may…" sentence is replaced by one real, optional, post-result pilot-interest email handoff with fixed copy across every situation (heading "Interested in the Sutriva pilot?"; button "Register my interest"; success "Thanks — we've registered your interest. We'll email you when the Sutriva pilot is ready."). The form renders only inside the result screen, is never required to finish a check or explore another situation, and collects exactly one field — no name, phone number or consent checkbox. A valid submission is persisted server-side (`POST /v1/situation-pilot-interest`; table `situation_pilot_interest`, migration `0007_situation_pilot_interest`) with the situation and a submission timestamp; a repeat of the same email for the same situation is idempotent (no duplicate row, same success copy); a database failure returns a real error and the frontend never fabricates the success screen. Origin validation and a per-IP rate limit protect the submit route, which 404s (indistinguishable from a nonexistent route) while `track11aEnabled` is false. Retrieval is operator-only: `GET /v1/situation-pilot-interest/admin/export` is excluded from the OpenAPI schema, unlinked from the frontend, and fails closed (404) until a `SITUATION_PILOT_INTEREST_ADMIN_TOKEN` is configured out of band, then requires an exact token match; this is the only surface anywhere that returns an email address, and it never appears in analytics, a URL, or a log line. Analytics gain exactly one new event type (`situation_pilot_interest_submitted`) and one new screen name per situation, distinct from Phase 1.1B's own, unrelated `pilot_interest_clicked`; submitting this form is never treated as consent to promotional updates, and Phase 1.1B's separate OTP/mobile-verification consent model, tables and flag are untouched. See `docs/execution/SITUATIONS_REDESIGN.md`. | Give the visitor a real, low-friction way to ask for a future invitation without collecting a phone number in 1.1A, while keeping the submission genuinely optional, genuinely persisted, and cleanly separate from Phase 1.1B's own, differently-scoped pilot consent flow | DECIDED |
